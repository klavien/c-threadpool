# thread_pool

the purpose is simple!

just make a fk thread pool as fast as posible 

easy to use!

use a lock free queue

mpmc support multithread safe(multi producer and multi consumer), it can't guarante FIFO

and spsc only used in single producer and single consumer scenes, it can guarante FIFO.

